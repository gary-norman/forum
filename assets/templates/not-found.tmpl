{{ define "not-found"}}

    <div class="container-content">
        <div class="content-error">
            <h1>Oops! 🤔</h1>
            <p>This {{.Message}} does not exist!</p>
            <button  class="btn-post btn-md btn-primary btn-icotext btn-action-primary btn-invert-onoff_onoff" onclick="window.location.href = '/';">
                <span class="btn-home">go home!</span>
            </button>
        </div>
    </div>

{{ end }}
