{{ define "loader" }}
  <div id="loader-backdrop">
    <div id="loader-modal">
    <!-- <div class="spinner"></div> -->
    <span class="loader"></span>
      <p class="loader-text">Loading</p>
    </div>
  </div>
{{ end }}
